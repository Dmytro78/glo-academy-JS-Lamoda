// 'use strict';
// // import getGoods from ".modal-basket";

/*

try {

    if (document.querySelector('.card-good')) {
        throw 'This is not a good page';
    }
    
    const cardGoodImage = document.querySelector('.card-good__image');
    const cardGoodBrand = document.querySelector('.card-good__brand');
    const cardGoodTitle = document.querySelector('.card-good__title');
    const cardGoodPrice = document.querySelector('.card-good__price');
    const cardGoodColor = document.querySelector('.card-good__color');
    const cardGoodColorList = document.querySelector('.card-good__color-list');
    const cardGoodSize = document.querySelector('.card-good__sizes');
    const cardGoodSizeList = document.querySelector('.card-good__sizes-list');
    const cardGoodBuy = document.querySelector('.card-good__buy');

    const renderCardGood = ([{ cost, brand, name, color, sizes, photo }]) => {
        console.log(brand);
    };

    getGoods(renderCardGood, 'id', hash);

} catch (err) {
    console.warn(err);
}
*/